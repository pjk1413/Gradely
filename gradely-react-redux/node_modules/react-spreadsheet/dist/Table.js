import React from "react";
import { range } from "./util";

var Table = function Table(_ref) {
  var children = _ref.children,
      columns = _ref.columns,
      hideColumnIndicators = _ref.hideColumnIndicators;
  var columnIndicatorsShown = !hideColumnIndicators;
  var columnNodes = range(columns + Number(columnIndicatorsShown)).map(function (i) {
    return React.createElement("col", {
      key: i
    });
  });
  return React.createElement("table", {
    className: "SpreadsheetTable"
  }, React.createElement("colgroup", null, columnNodes), React.createElement("tbody", null, children));
};

export default Table;